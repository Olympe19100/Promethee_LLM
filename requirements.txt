# =============================================================================
# PROMETHEE - Requirements
# =============================================================================

# Core ML - torch is pre-installed in the base image, do NOT override it
# torch is provided by the Docker base image (pinned to CUDA version)
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0

# Quantization (for teacher model)
bitsandbytes>=0.42.0

# Mamba SSM - installed separately in Dockerfile (requires CUDA build)
# causal-conv1d>=1.2.0
# mamba-ssm>=1.2.0

# SVM & Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0

# Data processing
beautifulsoup4>=4.12.0
lxml>=5.0.0
sec-edgar-downloader>=5.0.0
numpy>=1.24.0
pandas>=2.0.0

# Utilities
tqdm>=4.66.0
loguru>=0.7.0

# Geometric / TDA
networkx>=3.0.0
ripser>=0.6.0

# Visualization
tensorboard>=2.15.0
matplotlib>=3.8.0

# RunPod
runpod>=1.6.0

# ONNX export (optional)
onnx>=1.14.0
onnxruntime>=1.16.0
